# CI-file protection (#126): PRs touching these paths require code-owner
# review, which a PR author cannot self-provide — so an agent session cannot
# modify the verification machinery it runs under. Routine PRs (content,
# pages, tests, deps) need no review (base requirement is zero approvals).
# Escape hatch for legitimate CI maintenance: an auditable admin action
# (temporarily lifting enforcement), i.e. the same irreducible admin boundary
# /trust names. ADR 0022 lists delivery/DECLARED alongside verify-config.
/.github/ @briananderson1222
/scripts/ @briananderson1222
/delivery/DECLARED @briananderson1222
